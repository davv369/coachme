/**
 * Status of training session
 * Currently not used, but reserved for future use (e.g., IN_PROGRESS, CANCELLED)
 */
export enum TrainingSessionStatus {
  COMPLETED = 'COMPLETED',
  IN_PROGRESS = 'IN_PROGRESS',
  CANCELLED = 'CANCELLED',
}
